import React from "react";
// import PropTypes from "prop-types";

const about = () => {
    return (
        <div style={{ minHeight: "calc(100vh - 108.5px)" }}>
            As/is, nothing really much to say here. Probably will update who I
            am in the future.
        </div>
    );
};

// about.propTypes = {};

export default about;
