import React from "react";
import PropTypes from "prop-types";

const Header = props => {
    return (
        <header>
            <button>log in</button>
        </header>
    );
};

Header.propTypes = {};

export default Header;
